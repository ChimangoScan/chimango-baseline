# Python dependencies for the analysis pipeline (analysis/).
# Tested with Python 3.12.3.
#
# sqlite3 is part of the Python standard library (no install needed).
# PyMongo is intentionally omitted: it is needed only to draw a different sample
# from a private crawl and is not part of artifact evaluation.

matplotlib==3.8.4    # figures (analysis/make_figs.py, analysis/analyze_extra.py)
numpy==1.26.4        # figures (analysis/make_figs.py, analysis/analyze_extra.py)
zstandard==0.25.0    # stream-decompress the released reports database
